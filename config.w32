// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("pgdal", "for pgdal support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("pgdal", "enable pgdal support", "no");

if (PHP_PGDAL != "no") {
	EXTENSION("pgdal", "pgdal.c");
}

